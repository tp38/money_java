@startuml "money java activités"

start
:welcome;
:enable menu items;
note left
    Comptes
    Categories
    Recherche
end note
repeat
    split
        split
            -> "Comptes" item;
            :accounts;
        split again
            -> "Categories" item;
            :categories;
        split again
            -> "Recherche" item;
            :search;
        end split
        :operation list;
    split again
        -> "Aide" item;
        :help;
    split again
        -> "Quitter" item;
        #Green:(A)
        detach
    end split
repeat while ( close window ? )
-> Yes;
#Green:(A)
if(replicate_btn and server == local and accessible) then (yes)
    :replicate;<<#Yellow>>
endif
end
@enduml